void example() {
  do {
    skip;
    lock_return();
    skip;
    if ($\alert<3>{\top}$) {
      skip;
      unlock_return();
      skip;
      if($\top$) {
        skip;
      } else {
        skip;
      }
      skip;
    } 
  } while ($\top$);
  skip;
  unlock_return();
}
